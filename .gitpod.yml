image: gitpod/workspace-postgres

tasks:
  - name: Setup
    init: >
      yarn install
      cp .env.example .env
    command: >
      yarn migrate:dev
      yarn codegen:graphql --watch
  - name: Dev Server
    command: yarn dev

ports:
  - port: 3000
    onOpen: open-preview
