image: node:latest

services:
  - postgres:latest

stages:
  - test
  - build

cache:
  paths:
    - node_modules/

before_script:
  - npm install

Typecheck:
  stage: test
  script:
    - cp .env.example .env
    - npm run generate
    - npm run typecheck

Lint:
  stage: test
  script:
    - npm run lint

Build:
  stage: build
  script:
    - npm run build
