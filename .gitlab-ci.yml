image: node:latest

services:
  - postgres:latest

stages:
  - test

cache:
  paths:
    - node_modules/

before_script:
  - npm install
  - cp .env.example .env

Lint:
  stage: test
  script:
    - npm run lint
  artifacts:
    expire_in: 1 day
